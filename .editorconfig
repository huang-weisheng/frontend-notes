root = true

# 匹配所有文件
[*]
# 设置文件编码为 UTF-8
charset = utf-8
# 缩进风格使用制表符(tab)
indent_style = tab
# 缩进大小为4列
indent_size = 4
# 换行符使用 LF (Unix风格)
end_of_line = lf
# 文件末尾插入空行
insert_final_newline = true
# 删除行尾空格
trim_trailing_whitespace = true

